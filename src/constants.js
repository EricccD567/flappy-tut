import { k } from './kaboomCtx';

export const textColor = k.color(0, 0, 0);
export const pipeColor = k.color(125, 206, 235);

export const fullHeight = k.height();
export const fullWidth = k.width();
export const halfHeight = k.height() / 2;
export const halfWidth = k.width() / 2;

export const PLAYER_SCALE_FACTOR = 0.5;
export const PLAYER_JUMP = 650;
export const GRAVITY = 2000;
export const BOUNDARY_BUFFER = 25;

export const PIPE_SCALE_FACTOR = 2;
export const PIPE_GAP = 250;
export const PIPE_SPEED = 150;
export const PIPE_RAND = 75;
